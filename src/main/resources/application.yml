server:
  port: 10831
logging:
  level:
    org.springframework: debug

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1/cspm?createDatabaseIfNotExist=true
    username: ch4njun
    password: cks14579!
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    show-sql: true
    generate-ddl: true
    database: mysql
    hibernate:
      ddl-auto: update
